<div class="mb-3 {{$mdClass}}">
    <button type="{{$type}}" class="btn btn-{{$btnColor}} btn-sm {{$class}}" style="margin-top: 25px; width: 100%;">@if($icon)<i class="{{$icon}}"></i>@endif {{$name}}</button>
</div>
